import {admin,bindings,get} from '@/lib/server';
export async function GET(req:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;try{const media=await get(id);if(!media||media.kind!=='media')return new Response('Imagem não encontrada',{status:404});if(media.data.visibility!=='public'){try{await admin();}catch{return new Response('Imagem não encontrada',{status:404});}}const file=await bindings().BUCKET.get(id);if(!file)return new Response('Imagem não encontrada',{status:404});return new Response(file.body,{headers:{'Content-Type':media.data.mime,'X-Content-Type-Options':'nosniff','Cache-Control':media.data.visibility==='public'?'public, max-age=3600':'private, no-store'}});}catch{return new Response('Imagem indisponível',{status:503});}}
